import React, { Component } from 'react';

class Restfulservice extends Component {
    render() {
        return (
            <div>
               <button onClick={this.handleClick}>API</button> 
            </div>
        );
    }
}

export default Restfulservice;